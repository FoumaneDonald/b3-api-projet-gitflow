name: CI - Basic Checks

# Déclenche le workflow sur toute Pull Request vers develop
on:
  pull_request:
    branches: [develop]

jobs:
  test:
    name: Simple Test Job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Test GitHub Actions
        run: echo "Workflow detected and running successfully!"
